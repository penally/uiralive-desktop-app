import type { MediaSource } from '../lib/types';

// Add your own servers here. Each entry should match the MediaSource shape.
// Example:
// export const customServers: MediaSource[] = [
// 	{ url: 'https://example.com/stream.m3u8', quality: '1080P', name: 'My CDN', speed: 'Fast', size: '—' }
// ];

export const customServers: MediaSource[] = [];

